name: workflowevents
on:
  pull_request:
  workflow_dispatch:
jobs:
    echohello:
      runs-on: ubuntu-latest
      steps:
        - name: echo event name
          run: echo "Event name is ${{ github.event_name }}"    