name: workflowevents
on:
  push:
  pull_request:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
    echohello:
      runs-on: ubuntu-latest
      steps:
        - name: echo event name
          run: echo "Event name is ${{ github.event_name }}"    